extends ../../layouts/default.pug

block styles
  link(rel="stylesheet", href="/client/css/login/styles.css")

block main
  .wrapper
    //- Form Login
    .login_box.form-container#login
      .login-header
        span Login
      .input_box
        input#user.input-field(type="text" required)
        label(for="user" class="label") Username
        i.bx.bx-user.icon
      .input_box
        input#password.input-field(type="password" required)
        label(for="password" class="label") Password
        i.bx.bx-lock-alt.icon
      .remember-forgot
        .remember-me
          input#remember(type="checkbox")
          label(for="remember") Remember me
        .forgot
          a(href="#") Forgot password?
      .input_box
        input.input-submit(type="submit" value="Login")
      .register
        span Don't have an account?
        a(href="#" onclick="showRegister()") Register

    //- Form Register
    .register_box.form-container#register
      .register-header
        span Register
      .input_box
        input#user.input-field(type="text" required)
        label(for="user" class="label") Username
        i.bx.bx-user.icon
      .input_box
        input#email.input-field(type="email" required)
        label(for="email" class="label") Email
        i.bx.bx-envelope.icon
      .input_box
        input#password.input-field(type="password" required)
        label(for="password" class="label") Password
        i.bx.bx-lock-alt.icon
      .terms-conditions
        input#terms(type="checkbox")
        label(for="terms") I agree to the terms and conditions
      .input_box
        input.input-submit(type="submit" value="Register")
      .login
        span Already have an account?
        a(href="#" onclick="showLogin()") Login

  script(src="/client/js/script.js")
