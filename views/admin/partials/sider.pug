aside
    .top 
        .logo 
            h2 3 
                span.danger Wings
            
        .close#close_btn
            span.material-symbols-sharp close
    .sidebar
        a.active(href=`/${prefixAdmin}/dashboard`) 
            span.material-symbols-sharp grid_view 
            h3 Tổng quan
        //- if(role.permissions.includes("products-category_view"))
    
        //- a(href=`/${prefixAdmin}/products-category`) Danh mục sản phẩm
        //- if(role.permissions.includes("products_view"))
        
        //- a(href=`/${prefixAdmin}/products`) Sản phẩm
        //- if(role.permissions.includes("roles_view"))
        
        //- a(href=`/${prefixAdmin}/roles`) Nhóm quyền
        //- if(role.permissions.includes("permission_view"))
    
        //- a(href=`/${prefixAdmin}/roles/permissions`) Phân quyền
        //- if(role.permissions.includes("accounts_view"))
    
        a(href=`/${prefixAdmin}/accounts`) 
            span.material-symbols-sharp person_outline 
            h3 Tài khoản
    
        a(href=`/${prefixAdmin}/settings/general`) 
            span.material-symbols-sharp settings
            h3 Cài đặt chung

        a(href=`/${prefixAdmin}/logout`)
            span.material-symbols-sharp logout
            h3 logout
